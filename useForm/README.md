# useForm Hook

```
    Ejemplo de uso:
    const initialForm = {
        name: '',
        edad: 0,
        email: ''
    }
    const [formState, handleInputChange, reset] = useForm( initialForm )

```